# Revision service (:8000). Chỉ service giữ khóa model.
REVISION_SERVICE_PORT=8000
STUDIO_ORIGIN=http://localhost:3000
OPENAI_API_KEY=
OPENAI_MODELS=
# Bắt buộc để bật /debug/*: chuỗi ngẫu nhiên ≥ 24 ký tự, ví dụ
# node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
REVISION_DEBUG_TOKEN=
# REVISION_MODEL_MODE=mock   # chỉ dùng cho eval giả lập
# REVISION_NODE_CACHE=0      # tắt cache node
